{
  "name": "example3",
  "version": "1.0.0",
  "description": "Task: Клиент. Выбрать две фичи из ES Next и настроить трансформацию через плагины для того чтобы эти фичи работали в последней версии хрома.",
  "main": "index.js",
  "scripts": {
    "build-lib": "babel ./lib --out-dir .build/lib/",
    "build-babel": "rimraf .build/ && npm run build-lib && babel ./client --out-dir .build/client/ --copy-files",
    "build-webpack": "rimraf .build2/ && webpack --config webpack.config.js --mode development",
    "build-webpack-babel": "npm run build-babel && rimraf .build2/ && webpack --config webpack2.config.js --mode development"
  },
  "author": "alfixed",
  "license": "MIT",
  "devDependencies": {
    "babel-preset-es2015": "^6.24.1",
    "copy-webpack-plugin": "^4.5.1",
    "rimraf": "^2.6.2",
    "webpack": "^4.6.0",
    "webpack-cli": "^2.1.2",
    "babel-cli": "^6.26.0"
  },
  "dependencies": {
    "array.prototype.flat": "^1.2.1",
    "babel-polyfill": "^6.26.0"
  }
}
